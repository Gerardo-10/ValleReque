{% block customCSS %}
    <link rel="stylesheet" href="{{ url_for('static', filename='css/terrenos.css') }}">
    <link rel="stylesheet" href="{{ url_for('static', filename='css/clientes.css') }}">
{% endblock %}

{% block body %}
    <div class="container">
        <div class="contenedor-principal">
            <header class="header">
                <div class="header-content">
                    <div class="header-icon">
                        <i class="fa-solid fa-location-dot"></i>
                    </div>
                    <h1>Terrenos</h1><br>
                </div>
            </header>
            <br>
            <!-- Barra de búsqueda y filtros -->
            <div class="barra-herramientas">
                <div class="busqueda-container">
                    <input type="text" id="buscarTerreno" placeholder="Buscar terreno">
                    <button class="btn-buscar">
                        <i class="fas fa-search"></i>
                    </button>
                </div>
                <div class="filtro-container">
                    <select id="filtroTerrenos">
                        <option value="">Filtrar por...</option>
                        <option value="proyecto">Proyecto</option>
                        <option value="etapa">Etapa</option>
                        <option value="unidad">Unidad</option>
                    </select>
                </div>
                <div class="filtro-container">
                    <select id="filtroTerrenosEstado">
                        <option value="todos">Todos</option>
                        <option value="disponible">Disponible</option>
                        <option value="enProceso">En Proceso</option>
                        <option value="reservado">Reservado</option>
                        <option value="noDisponible">No Disponible</option>
                        <option value="vendido">Vendido</option>
                    </select>
                </div>
                <button class="btn-eliminar">
                    <i class="fa-solid fa-trash"></i>
                </button>
            </div>

            <!-- Tabla de terrenos -->
            <div class="tabla-container">
                <table class="tabla-terrenos" id="tabla_terrenos">
                    <thead>
                    <tr>
                        <th>ID</th>
                        <th>Proyecto</th>
                        <th>Etapa</th>
                        <th>Unidad</th>
                        <th>Área</th>
                        <th>Precio</th>
                        <th>Tipo</th>
                        <th>Estado</th>
                        <th>Acciones</th>
                    </tr>
                    </thead>
                    <tbody id="tabla_terrenos_body">
                    {% for terreno in terrenos %}
                        <tr data-id="{{ terreno.id_empleado }}" data-estado="{{ terreno.estado | lower }}">
                            <td>{{ terreno.id_terreno }}</td>
                            <td>{{ terreno.nombre_proyecto }}</td>
                            <td>{{ terreno.etapa }}</td>
                            <td>{{ terreno.unidad }}</td>
                            <td>{{ terreno.area }}</td>
                            <td>{{ terreno.precio }}</td>
                            <td>{{ terreno.tipo }}</td>
                            <td>{{ terreno.estado }}</td>
                            <td class="acciones">
                                <button class="btn-editar-terreno" id="btnEditarTerreno"
                                        data-id="{{ terreno.id_terreno }}">Editar
                                </button>
                                <button class="btn-eliminar-terreno" id="btnEliminarTerreno"
                                        data-id="{{ terreno.id_terreno }}">Eliminar
                                </button>
                            </td>
                        </tr>
                    {% endfor %}

                    </tbody>
                </table>
            </div>

            <!-- Botones de acción -->
            <div class="acciones-container">
                <button class="btn-agregar" id="btnAgregarTerreno">Agregar</button>
            </div>
        </div>
    </div>

    <!-- Modal para agregar terreno -->
    <div class="modal-terreno" id="modalAgregarTerreno">
        <div class="modal-content">
            <div class="modal-header">
                <div class="modal-title">
                    <div class="icon-circle">
                        <i class="fas fa-map"></i>
                    </div>
                    <div>
                        <h2>Nuevo Terreno</h2>
                        <p>Bienvenido a la creación de terreno. No olvide que los campos deben estar en proyecto creado,
                            si el campo no existe debe crearlo en la sección de catálogos.</p>
                    </div>
                </div>
                <span class="close" id="btn-cancelar-terreno">&times;</span>
            </div>
            <div class="modal-body">
                <form id="formAgregarTerreno">
                    <div class="form-group">
                        <label for="proyecto">Selecciona el Proyecto*</label>
                        <select id="proyecto" name="proyecto" required>
                            <option value="">Seleccionar...</option>
                            <option value="1">Valle Reque</option>
                        </select>
                    </div>
                    <div class="form-group">
                        <label for="etapa">Selecciona la Etapa*</label>
                        <select id="etapa" name="etapa" required>
                            <option value="">Seleccionar...</option>
                            <option value="1">1</option>
                            <option value="2">2</option>
                        </select>
                    </div>
                    <div class="form-group">
                        <label for="manzana">Selecciona la Manzana*</label>
                        <select id="manzana" name="manzana" required>
                            <option value="">Seleccionar...</option>
                            <option value="A">A</option>
                            <option value="B">B</option>
                        </select>
                    </div>
                    <div class="form-group">
                        <label for="tipoTerreno">Tipo de terreno*</label>
                        <select id="tipoTerreno" name="tipoTerreno" required>
                            <option value="">Seleccionar...</option>
                            <option value="Calle">Calle</option>
                            <option value="Avenida">Avenida</option>
                            <option value="Esquina">Esquina</option>
                            <option value="Parque">Parque</option>
                            <option value="Esquina_Parque">Esquina-Parque</option>
                        </select>
                    </div>
                    <div class="form-group">
                        <label for="area">Área m²*</label>
                        <input id="area" name="area" type="number" required>
                    </div>
                    <div class="form-group">
                        <label for="precio">Precio*</label>
                        <input type="number" id="precio" name="precio" required>
                    </div>
                </form>
            </div>
            <div class="modal-footer">
                <button class="btn-secondary" id="btn-cancelar-terreno-footer">Cancelar</button>
                <button class="btn-primary" id="btn-guardar-terreno">Guardar</button>
            </div>
        </div>
    </div>

    <!-- Overlay para modales -->
    <div class="modal-overlay-terreno" id="modalOverlay"></div>

    <script src="{{ url_for('static', filename='js/terrenos.js') }}"></script>
{% endblock %}